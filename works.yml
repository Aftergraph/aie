# Diagnostic WORKS probe: private Runtime SSH access from avc-core.
# Proof-only; do not merge.
version: 1

work:
  verify:
    triggers: [push, pull_request]
    requirements:
      confidence: development
      os: linux
      arch: amd64
      pool: avc-core
    nodes:
      runtime-private-access:
        run: |
          set -euo pipefail
          export PATH=/opt/works/node-current/bin:/usr/local/go/bin:/usr/local/bin:/usr/bin:/bin:$PATH
          export GIT_SSH_COMMAND='ssh -o BatchMode=yes -o ConnectTimeout=10 -o StrictHostKeyChecking=yes'
          git ls-remote git@github.com:Aftergraph/runtime.git | grep -q 'd9c5efb3a74222326ec71b06d5c2df2d4215d68f'
          printf 'STEWARD_P2_RUNTIME_SSH_ACCESS=PASS\n'
        cache: false
        timeout_s: 120
